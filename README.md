# Botphp

## ✅ التعديلات الأخيرة المطلوبة

### 1) الألبوم الآن يُرسل كألبوم مجمّع
- إذا كان `Content ID` تابعًا لألبوم (`media_group_id`) سيتم الإرسال عبر `sendMediaGroup` كرسائل مجمعة.
- إذا تعذر (نوع غير مدعوم داخل media group) يتم fallback تلقائي بإرسال العناصر منفصلة.

### 2) القوائم المتداخلة بشكل واضح
- عند إنشاء زر من اللوحة:
  - هل هو **زر عادي ذو محتوى** أو **زر قائمة**؟
  - هل هو **في الرئيسية** أو **تابع لقائمة**؟
- إذا كان تابعًا لقائمة، تختار القائمة الأب مباشرة من قائمة منسدلة.

### 3) سلوك القائمة الفرعية بالمحتوى
- القائمة تقدر تحمل `menu_text` أو `Connect ID`.
- إذا كان المحتوى نص: يتم تحديث الرسالة.
- إذا كان المحتوى ميديا/ألبوم: تُحذف الرسالة القديمة وتُرسل رسالة/ألبوم جديد مع أزرار القائمة.
- يوجد زر ثابت للعودة للرئيسية `🏠 الرئيسية` مهما كان عمق التصفح.

### 4) ألوان الأزرار + custom emoji
- تم تطبيق Bot API 9.4 في الـ Inline Keyboard:
  - `style`: `primary` / `success` / `danger`
  - `icon_custom_emoji_id`
- ويمكن جلب `icon_custom_emoji_id` عبر `@GetEmojiIdBot`.

### 5) ترتيب الأزرار
- سحب وإفلات داخل شجرة متداخلة (يمين/يسار/فوق/تحت).
- كل صف داخل الكيبورد محدود بـ **4 أزرار أفقيًا**.
- التوزيع مرن: مثل `3-2-1` أو `1-3-1` أو أي توزيع.

## التشغيل
Webhook:
`https://api.telegram.org/bot8432282773:AAHZmuIMka4qU6attueWz4Nk0TXrG4HUJJs/setWebhook?url=https://YOUR-DOMAIN.COM/bot/index.php`

Admin:
`https://YOUR-DOMAIN.COM/bot/admin.php`
